# ER-002/r1 — Ерлан

- Message-ID: ER-002
- Revision: 1
- Role: ERLAN
- Task: T-09
- Status: needs_review (README); сравнение кандидата не выполнено из-за пропущенного контрольного окна
- Automation-ID: none (новый этап вручную, расписание не создавалось)
- Automation-state: not_applicable
- Source-commit: `eb03bf4afc644882a7e8431d38cb51e83d4dccc0`
- Tested-commit: `4272a0787248de1481fb6b45dfcbdecee1568ae4` (baseline, проверен отдельно ниже)
- Working-branch: `test/T-09-erlan-next-round`
- Updated: 2026-09-23 16:20 UTC+05

## Result

Получено активное ER-002/r1. Создана отдельная чистая ветка от указанного
T-07 Base. README обновлён для актуального main: один порядок установки,
официальные файлы вне Git, компоненты, проверенные команды и mock-ограничения.
Демо-сценарий синхронизирован с текущими 100-seed агрегатами и исключает
неизолированное сравнение двух командных ревизий; живая репетиция не заявлена.
Начало после контрольного срока 16:10; seed 100–199 мной ранее не
использовались (мои опубликованные сравнения — seed 0–9). Использование этого
диапазона другими участниками не подтверждаю. Frozen candidate hash и допуск
координатора до начала этой проверки не получены; контрольный эксперимент не
запускался. Новую стратегию не принимал и baseline не менял.

## Files changed

- `coordination/tasks/T-09.md`
- `docs/evaluation/ER-002-r1.md`
- `README.md`
- `docs/evaluation/demo.md`

## Validation

- `git rev-parse --show-toplevel`, `git branch --show-current`, `git status
  --short --branch`, `git remote -v`, `git config user.name`,
  `git config user.email` — exit 0; ветка T-05 чистая, автор Erlan Mereev.
- `git fetch origin --prune` — exit 0; координатор обновлён до `eb03bf4`.
- `git show origin/feat/T-03-nursultan-agent:coordination/CONTROL.md` и
  `:coordination/tasks/T-09.md`, `:coordination/inbox/ERLAN.md` — exit 0;
  active run/revision3, T-09 requested, ER-002/r1 requested.
- `git worktree add -b test/T-09-erlan-next-round ... 025d72a` — exit 0;
  старое дерево T-05 сохранено.
- `python3.12 -m venv /private/tmp/beeline-t09-kwKYV3/.venv` — exit 0;
  первый `pip install -r requirements.txt` в sandbox — exit 1 (DNS);
  повтор с разрешённым сетевым доступом — exit 0, Python 3.12.14,
  pandas 3.0.1, numpy 2.3.5.
- Извлечённые локальные 13 официальных файлов временно перепакованы в
  `/private/tmp/beeline-t09-kwKYV3/local-case-repacked.zip` (не Git);
  `git archive 025d72a | tar -x -C /private/tmp/beeline-t09-kwKYV3/stage` — exit 0;
  `python scripts/prepare_case.py <local-case-repacked.zip>` — exit 0,
  13 файлов установлены. ZIP собран из ранее извлечённых файлов, не исходный
  архив организатора; его SHA-256 не является контрольным hash официального ZIP.
- В отдельном stage, `python -m unittest scripts.test_agent
  strategy.tests.test_core evals.test_contract_checks -q` — exit 0, 35/35.
- Там же `python local_eval.py` — exit 0, PASS seed42, net +327 917,
  расходы 99 998, 20 пилотов и 10 финальных кампаний (runner показывает
  30 действий вместе с пилотами).
- `python evals/check_agent.py --agent agent.py --package . --feedback-check`
  — exit 0, решения различаются, обе raw errors=[].
- `python make_submission.py` — exit 0, 10 кампаний; `shasum -a 256
  submission.csv` — `305c7d41394309b68287c035f2c89fcd08875dede274df40f08de9241e65e46a`;
  `cmp -s` с CSV текущей ветки — exit 0, файл побайтно идентичен.

## Findings / Known limitation

Время передачи README 16:15 уже наступило при получении задания. Инструмент
изоляции двух стратегий не был готов к 16:10; риск общего `sys.modules` в
старом `evals/compare_agents.py` остаётся, поэтому этим инструментом не
проверял экспериментальную ревизию. Контрольные seed 100–199 не запускались.
Локальный архив для проверки установщика реконструирован из извлечённых
файлов; подлинный ZIP на этой машине отсутствует. Новый прогон — macOS, не
Windows/Linux. Исходные данные, ключи и CSV из stage в Git не добавлялись.
Пятиминутный сценарий документально обновлён, но фактическая репетиция
участниками не подтверждена.

## Integrator action

Нурсултану: принять README как документационную передачу после сверки diff;
сохранить baseline `4272a07` и его CSV. Не засчитывать независимое сравнение
нового кандидата как выполненное и не принимать кандидата по этому отчёту.
Инструмент изоляции и контроль после пропущенного окна требуют отдельного
решения о сроке/задаче; пороги не ослаблять.
