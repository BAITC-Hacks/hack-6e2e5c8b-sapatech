# Повторная проверка интеграции `9f782f1`

Дата: 2026-09-23. После изменения загрузки истории в `agent.py` коммитом
`615251b` повторно проверен tip ветки Нурсултана `9f782f1`. Код взят из
этого коммита и размещён в отдельной временной копии официального пакета.
Общий `submission.csv`, официальные scorer и рабочие ветки коллег не менялись.

Команды из [первого интеграционного отчёта](integrated-f95a8e3.md) запущены
заново с `<staged>/agent.py` и `strategy/` из `9f782f1`; дополнительно
выполнено `python -m unittest discover -s scripts -p test_agent.py -v`
из копии командного репозитория. Результаты:

- T-03: 12/12 тестов прошли, включая новый тест поиска истории в отдельной
  официальной папке; T-05: 8/8 тестов прошли.
- Seed 42: 10 исходных кампаний, 20 пилотов, 11 000 финальных контактов,
  99 998 стоимости финальных контактов, 0 raw-ошибок. Предупреждения о
  частичном cap и 371 повторном финальном контакте сохраняются.
- Public stub +0.5 против −0.5: 4 против 10 финальных кампаний, raw-ошибок
  в обоих случаях нет.
- Seed 0–9: 10/10 завершены, 0 raw-ошибок команды, 9/10 положительных,
  средний net ARPU 533 014.48, медиана 575 915.02, минимум −282 662.44
  (seed 2), максимум 943 807.80, средняя стоимость 99 823.00. В каждом
  из 10 seed net ARPU совпал с замером `f95a8e3`. Предупреждения есть на
  9 seed; они не являются ошибками формата.
- Повторный seed 2: нормализованный JSON кампаний и контактов/затрат совпал.
- Официальный `make_submission.py` дважды создал по 10 кампаний в
  изолированной копии; SHA-256 обоих CSV и прежнего результата `f95a8e3`
  совпал: `305c7d41394309b68287c035f2c89fcd08875dede274df40f08de9241e65e46a`.

`compare_agents.py` всё ещё возвращает код 1 из-за известного пустого
raw-ответа **официального template** на seed 3. У командного агента ошибок
нет. Время `Agent.act` в этом прогоне в среднем 1.83 с, максимальный полный
вызов evaluator 2.10 с; эти числа зависят от машины. Mock не предсказывает
скрытый балл. PowerShell/Windows остаётся непроверенным.
