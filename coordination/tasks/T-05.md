# T-05 — Ерлан: проверка и воспроизводимость

- **Status:** in_progress
- **Owner:** Ерлан
- **Branch:** `test/T-05-erlan-evaluation`
- **Base:** завершённый план `docs/T-02-team-plan`
- **Timebox:** проверки до 14:50; сравнительный отчёт до 16:15; финал до 17:15 UTC+05

## Outcome

Независимые проверки требований, честное сравнение стратегий, запускаемый README
и короткий сценарий демонстрации. Ошибки передаются владельцам с воспроизводимым примером.

## Owned paths

- `evals/**`
- `docs/evaluation/**` — агрегированные результаты без клиентских строк
- `README.md`
- `coordination/tasks/T-05.md`

## Acceptance criteria

- [ ] Проверен raw-output Agent.act до обрезания/исправлений official sanitizer.
- [ ] Проверяются 1–10 кампаний, валидность тарифов/каналов/фильтров и все лимиты.
- [ ] Проверка на публичном stub показывает влияние pilot results на решение.
- [ ] Проверены пропуски, нулевой baseline, отрицательный эффект и истощение ресурсов.
- [ ] Template и новая стратегия сравнены на одинаковых 10 seed; есть среднее,
  медиана, минимум, разброс, runtime и расходы, а не только лучший результат.
- [ ] Повторный запуск seed и make_submission воспроизводим; файлы данных не коммитятся.
- [ ] README содержит фактически проверенные команды PowerShell и описание данных.
- [ ] Есть пятиминутное демо: ввод, пилоты, решение, ограничения, итог, оговорки mock.

## Dependencies and constraints

Начать по контракту со stub и официальным template, не ждать готового ядра.
После передачи интегрированного commit проверить его в согласованной рабочей
копии, не подменять agent.py на своей ветке и не переписывать scorer.
Без запросов платного API и без новых зависимостей до согласования с Нурсултаном.
Окончательный submission.csv генерирует Нурсултан; Ерлан сверяет результат в
отдельной локальной копии, чтобы не перезаписать общий артефакт.

## Validation / handoff

- First handoff: independent raw-answer checker, public pilot stub, focused
  regression tests, and same-seed comparison runner in `evals/`.
- Verified locally: `python -m unittest evals.test_contract_checks -v` (8 passed);
  `python evals/check_agent.py --agent <official agent_template.py> --package
  <extracted package> --seed 42` (raw answer valid: 2 final campaigns, 6
  pilots, 5,570 total contacts, 22,280 total cost).
- Template feedback stub: +0.5 versus -0.5 changes campaigns; positive case
  truncates two late campaigns, negative case returns zero final campaigns.
  These are template findings, not findings about the team's agent.
- Template self-comparison on seeds 0..9: exact same net values, mean
  -480,836.18; median -357,947.80; minimum -1,019,431.06; 0/10 positive.
  The mock is not a hidden-score predictor. Agent implementation is not yet
  published on this branch, so candidate comparison and CSV checks are pending.
- First commit: `96c4b71` (published on `test/T-05-erlan-evaluation`).
- Second handoff: baseline aggregates in `docs/evaluation/baseline.md`,
  documented evaluation commands in README, and a disposable-copy CSV
  checker. Official template's two CSV runs matched byte-for-byte with 2
  campaigns; SHA-256 recorded in baseline report. Windows PowerShell syntax
  is documented but cannot be executed in this macOS environment.
- Third handoff: same-seed comparison now audits each raw `Agent.act` result
  before official sanitizer and records agent-only runtime. Template seed 3
  returns zero final campaigns (contract expects 1..10); official evaluator
  still scores the six pilots. Self-comparison on seeds 0..9 returns exit 1
  for this raw baseline error while preserving the measured aggregate.
- Demo handoff: `docs/evaluation/demo.md` gives a five-minute sequence for
  inputs, public pilots, raw decision, same-seed aggregates, CSV repeatability,
  and explicit mock limitations. Team-result placeholders remain unfilled
  until an integrated commit is independently checked.
- Known limitation: mock — локальный ориентир, не доказательство hidden-score
- Integrator action: принять воспроизводимые баг-репорты и подтверждённые результаты
