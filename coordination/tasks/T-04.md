# T-04 — Бауыржан: математическое ядро

- **Status:** integrated at `cca147b`; phase 2 robustness checked; baseline retained
- **Owner:** Бауыржан
- **Branch:** `feat/T-04-bauyrzhan-strategy`
- **Base:** первый коммит передачи Бауыржану в `docs/T-02-team-plan`
- **Timebox:** первая версия за 45–60 минут; улучшение до интеграционного рубежа

## Outcome

Рабочий Python-пакет strategy с тремя экспортами из docs/contracts.md.
Нурсултан подключает пакет через agent.py; Бауыржан не ждёт его обёртку.

## Owned paths

- `strategy/**`, включая собственные сфокусированные тесты `strategy/tests/`
- `coordination/tasks/T-04.md`

## Acceptance criteria

- [x] build_candidates, choose_pilot, select_campaigns соответствуют контракту v1.
- [x] История формирует prior; пилоты реально меняют порядок/выбор кампаний.
- [x] Конечные оценки, безопасная обработка пустой истории и нулевого ARPU.
- [x] Кандидаты покрывают несколько сегментов/таргетов; нет подгонки под mock secrets.
- [x] Есть остановка исследования по ресурсам/времени и резерв под финальный план.
- [x] Учитываются цена канала, уже потраченные ресурсы, порядок и overlap кампаний.
- [x] Результат содержит 1–10 допустимых кампаний; одинаковые входы дают одинаковый выход.
- [x] Сфокусированные проверки проходят; первая рабочая версия оформлена отдельным коммитом T-04.

## Dependencies and constraints

Читать HACKATHON.md, docs/contracts.md, ADR-003 и личную инструкцию.
Использовать собственную локальную копию официального пакета; не коммитить данные.
Не менять agent.py, README, evaluator, shared contract или зависимости.
При уточнении контракта передать конкретное предложение Нурсултану.
Не тратить время на UI, API-обвязку, GPU или сложную модель до рабочего baseline.

## Validation / handoff

- **Files changed:** `strategy/__init__.py`, `strategy/core.py`,
  `strategy/README.md`, `strategy/tests/test_core.py`,
  `strategy/tests/official_smoke.py`, `coordination/tasks/T-04.md`.
- **Implementation:** current tariff × ARPU candidates; robust historical priors;
  precision-weighted public pilots; repeated confirmation after initial exploration;
  deterministic greedy marginal portfolio with channel prices, ID order, caps,
  final-campaign overlap and estimated pilot coverage. All assumptions are in
  `strategy/README.md`. Only standard library, pandas and numpy are used.
- **Unit validation:** `python -m unittest discover -s strategy/tests -v` —
  14 passed. Includes zero/invalid historical denominators, empty history,
  missing segments, input immutability, changed decisions under changed pilot
  evidence, actual sample sizes, no double channel multiplier, final resource
  reserve, paid channels, overlap, negative fallback and sorted-ID cap behavior.
- **Official integration:** from a local extracted official participant package,
  `PYTHONPATH=/path/to/repository python /path/to/repository/strategy/tests/official_smoke.py --runs 10`.
  Calls unmodified `local_eval.evaluate_agent` through a test-only adapter;
  validates raw final output before sanitation, budget/contact limits, pilots,
  runtime and repeated-seed equality of campaigns and public observations.
- **Final ten-seed results (0–9):** 9/10 positive; median net gain 575,915.02;
  minimum -282,662.44 (seed 2); maximum 943,807.80. 20 pilots/run, 4–10 final
  campaigns, contacts <=15,000 and cost <=100,000 in every run. Strategy runtime
  1.51–1.83 seconds/run in this local environment. Repeat of seed 9 matched.
- **Submission seed smoke (42):** net gain 327,917.04; 15,000 contacts;
  cost 99,998; 20 pilots; 10 final campaigns; runtime 1.75 seconds. Repeated
  decisions and public observations matched. This is not submission.csv validation.
- **Environment:** Python 3.9, pandas 2.3.3, numpy 2.0.2 in a temporary venv;
  organizer data extracted outside the repository. No datasets or dependencies
  added to the repository. Initial exploratory version was less stable; numbers
  above refer to the version with confirmation and 200-contact pilot requests.
- **Commit:** `feat(strategy): implement pilot-aware campaign core [T-04]`;
  this task card is included in that commit. The operator explicitly authorized
  commit and push of `feat/T-04-bauyrzhan-strategy`. Git author verified as
  Bauyrzhan Myrzagaliyev. No merge/rebase/reset or changes to main.
- **Limitations:** hidden judging unavailable; history does not identify causal
  conversion. Cross-channel extrapolation and pilot overlap are approximations.
  One negative seed remains; positive net gain is not guaranteed. Production
  `Agent.act`, bounded pilot error recovery and submission generation are T-03.
  Empty audiences/zero campaign limits cannot yield an executable nonempty plan.
- **Integrator action:** Нурсултан imports the three exports from `strategy`,
  loads history, executes returned pilot requests and refreshes resources after
  each call. Official profile attribute is `env.customer_profile`, not the
  `env.profile` spelling in the contract introduction; no shared contract was
  edited. Ерлан runs independent checks and submission reproducibility after
  integration. Shared files and root agent.py were not changed.

## Этап 2 — устойчивость (2026-09-23)

Исходный алгоритм `cca147b` сохранён. По публичным наблюдениям локального
пилота в убыточных seed 2, 11, 12, 16 исследование выбирало 13–15 разных
гипотез за 20 push-пилотов; положительные и отрицательные измеренные lift
обычно лежали в пределах примерно ±0.1. Публичный шум 0.804 даёт стандартную
ошибку около 0.057 при 200 контактах. Поэтому максимум среди многих
наблюдений может быть случайным. Повторные пилоты частично снижают этот риск.
Перенос push-оценки на платные каналы по множителю не проверен прямыми
пилотами; история смены тарифов не доказывает причинный эффект кампании.
Текущая `error` описывает предположенный шум выборки, но не ошибку переноса
между каналами и не смещение из-за выбора победителя.

Проверено ровно одно изменение в отдельной временной копии `strategy/core.py`:
`ratio = mean - 1.0 * error` вместо `0.5 * error` в `select_campaigns`.
Исходная и новая версии получили одинаковые seed 0–9; seed 10–19 проверены
отдельно после первой группы. Официальный локальный evaluator не менялся.
Затраты — средний `total_cost`, время — среднее время работы стратегии,
нарушения — проверки сырого ответа, бюджета, контактов, пилотов и runtime.

| Seed | Версия | Средний net gain | Медиана | Минимум | Убыточных | Средние затраты | Средний runtime | Нарушения |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 0–9 | Исходная | 533 014.48 | 575 915.02 | -282 662.44 | 1/10 | 99 823.00 | 1.652 с | 0 |
| 0–9 | Штраф 1.0 | 213 588.40 | 232 166.97 | -100 343.86 | 1/10 | 22 230.80 | 1.616 с | 0 |
| 10–19 | Исходная | 399 673.39 | 256 420.23 | -63 864.92 | 3/10 | 94 983.20 | 1.526 с | 0 |
| 10–19 | Штраф 1.0 | 254 842.76 | 334 970.10 | -156 762.94 | 3/10 | 54 799.40 | 1.553 с | 0 |

Новая версия выиграла 1/10 пар в первой группе и 3/10 в отложенной;
средний результат снизился на 319 426.08 и 144 830.62. Убыточных запусков
не стало меньше, худший результат на отложенных seed ухудшился. **Изменение
отклонено**, `strategy/core.py` не менялся. Снижение затрат здесь не
компенсирует снижение результата. 20 пилотов выполнено на каждом seed;
проверяемых нарушений нет. Это локальный synthetic/mock, не скрытый judge.

Проверки: `python -m unittest discover -s strategy/tests -v` — 14/14;
повтор seed 9 и 19 дал одинаковые решения, публичные наблюдения и net gain.
Для воспроизведения в распакованном официальном пакете:
`PYTHONPATH=/path/to/repository python /path/to/repository/strategy/tests/official_smoke.py --runs 10`
и затем `--runs 20`; во втором выводе взять строки seed 10–19.
Для сравнения скопировать `strategy/` во временную папку, поменять только
указанную строку и запустить те же команды с копией в `PYTHONPATH`.
Среда: Python 3.9, pandas 2.3.3, numpy 2.0.2; данные организаторов в Git не
добавлялись.

Передача Нурсултану: взять исходный алгоритм `cca147b` без новой настройки;
таблица и команды выше фиксируют сравнение. Точный hash этого документирующего
коммита передаётся отдельно после push. Неизвестны результаты hidden judge;
20 seed не доказывают устойчивый положительный эффект.

## Очередь BAUYRZHAN_CORE — B1-001/r1 (2026-09-23)

Статус: **needs_review**. Проверен интеграционный коммит
`7c27cfe6a943ea468a69cf9c2b7d37feadceb58b` в отдельной временной копии.
Отчёт: [strategy/reports/B1-001-r1.md](../../strategy/reports/B1-001-r1.md).

На seed 0/42 воспроизведены 3747/371 повторных финальных контактов.
Ошибка двойного зачёта не подтверждена: дополнительные эффекты учитываются
по максимуму, все повторные расходы вычитаются. На seed 0 широкая SMS-кампания
даёт оценённые +54099.96 на новой аудитории и -13528 на повторах; на seed 42
повторы бесплатного push расходуют контакты, но не деньги. Это компромисс
целых фильтруемых групп, не доказательство глобальной оптимальности.

Два сфокусированных regression-теста и три контролируемых сценария аудита
прошли. Runtime Agent.act: 1.8014/1.8610 с; наблюдаемых нарушений нет.
Это расчёт дополнительной **оценённой** полезности по публичным данным,
не новый замер score. Алгоритм не менялся; 0–19 baseline и отклонённый опыт
не повторялись. Команды, таблицы и диагностический скрипт включены в отчёт.

Automation-ID/state: **unavailable** — инструмент планировщика в текущем чате
не предоставлен. Расписание не создано; выполнена ручная проверка очереди.
После публикации ожидать новую revision; B1-001/r1 повторно не выполнять.
