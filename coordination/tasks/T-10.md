# T-10 — Improve pilot-driven strategy on a new branch

- **Status:** complete; validated candidate authorized for branch commit/push
- **Owner:** Нурсултан with Codex, explicitly requested in the current session
- **Branch:** `feat/T-10-robust-strategy`
- **Base:** `ca44f89`
- **Last updated:** 2026-09-23

## Outcome

Improve expected net ARPU and resilience to noisy pilots while preserving the
official public interface, deterministic submission and all resource limits.
The initial request authorized a local experiment after the earlier team stop.
The user's 2026-09-23 follow-up explicitly authorizes committing and pushing the
improved branch to the existing team Git repository. It does not authorize
restarting team schedules, merging main, changing visibility or platform submission.

## Owned paths

- `strategy/**`: strategy, focused regression tests and experiment report
- `evals/compare_isolated.py`: reproducible process-isolated paired evaluation
- `evals/test_compare_isolated.py`: isolation/validation regression tests
- `coordination/tasks/T-10.md`
- `submission.csv`: regenerate only after candidate validation
- `jobs/t10/**`: ignored local experiment snapshots and evidence

This is explicitly scoped cross-owner strategy/evaluation work for the user's
improvement request. Call it out at handoff; preserve previous task cards.
No changes to official files, agent.py, dependencies or shared contract v1.

## Acceptance criteria

- [x] Baseline and candidate run in separate processes with verified import paths.
- [x] Explore on seeds 0–19; freeze code before validation on seeds 100–199.
- [x] Retain candidate only if validation mean net improves, loss count does not
  increase, and worst net does not worsen against baseline on paired seeds.
- [x] Existing tests, meaningful new regressions, raw-output/resource checks,
  feedback dependence and repeated CSV generation pass.
- [x] Report seed lists, experiment selection, paired outcomes and limitations.

## Dependencies and constraints

Read HACKATHON.md, docs/contracts.md and coordination/decisions.md. Use only
public data and pilot feedback. Do not inspect mock effect implementation or
hidden environment state. No tuning to mock tariff winners or seed values.
No edits to main, merge, visibility change or platform submission. Commit/push
of this feature branch is authorized by the later user request above.

## Validation and handoff

Exploration declared before reading results, seeds 0–19:

1. `confirm`: pool pilots per source channel before applying transfer uncertainty;
   start confirmation after 8 pilots, allow up to 800 contacts per candidate.
2. `regularized`: same changes, with historical prior precision equivalent to one
   200-contact pilot (`200 / PILOT_STD**2`) instead of prior SD 0.15.

Both variants are generic statistical changes, with no tariff/seed-specific rules.
Candidate selection will use these exploration results; seeds 100–199 remain
untouched until candidate code is frozen. Baseline snapshot is in ignored
`jobs/t10/baseline`; original 35 unit tests passed with Python 3.12.14,
pandas 3.0.1, numpy 2.3.5.

Exploration complete: baseline mean 466343.93, minimum -282662.44, 4/20 losses;
`confirm` mean 589588.16, minimum -70361.89, 1/20 loss; `regularized` mean
393953.58, minimum -122229.90, 2/20 losses. Select `confirm`; reject
`regularized` for lower mean net. Original 35 tests still pass.

Before running seeds 100–199, freeze candidate `strategy/core.py` SHA-256:
`b4f7126c4c1c2c7de17871c639306452a54b893e3d9fbf5685789b00deff37c3`.
Canonical LF/Git source hash verified at delivery:
`07d7e0849d31ae5adb27485e710e41e09405042ede1561b0740471084379ad1b`.
Committed and frozen source differ only in line endings.
Isolated candidate directory: `jobs/t10/confirm`; baseline: `jobs/t10/baseline`.
No candidate retuning after this holdout begins. If the acceptance gate fails,
retain the original implementation and document the failed experiment.

## Final validation and handoff

- **Result:** 100/100 completed for each revision on untouched seeds 100–199.
  Baseline mean 286700.61, median 228119.90, minimum -742519.79, 27 losses;
  candidate mean 629492.15, median 640489.07, minimum -389787.24, 5 losses.
  Candidate wins 76 paired seeds; declared acceptance gate passes. No raw,
  resource or diagnostic errors/fallbacks in either revision. Max full runtime
  8.02 s under concurrent evaluation; all official limits respected.
- **Files changed:** `strategy/core.py`, `strategy/README.md`,
  `strategy/tests/test_robustness.py`, `strategy/reports/T-10-robustness.md`,
  `strategy/reports/T-10-evidence.json`, `evals/compare_isolated.py`,
  `evals/test_compare_isolated.py`, `submission.csv`, this task card.
- **Tests:** `python -m unittest scripts.test_agent strategy.tests.test_core
  strategy.tests.test_robustness evals.test_contract_checks
  evals.test_compare_isolated -q` — 54/54 PASS.
- **Comparison:** `python evals/compare_isolated.py --baseline-dir
  jobs/t10/baseline --candidate-dir jobs/t10/confirm --seeds 100:200
  --output jobs/t10/holdout.json`. Executed as four concurrent paired blocks
  `100:125`, `125:150`, `150:175`, `175:200`, then merged in seed order with
  the same summary and acceptance functions. Source/input hashes agree across
  all blocks; strategy SHA-256 remains the pre-holdout frozen value above.
- **Harness recovery:** first complete run could not export infinite ancillary
  ROI. Fixed exporter and added regression; reran all seeds without changing
  strategy. Private bytecode caches and harness/validator hash checks prevent
  import contamination. Required scores/resources must remain finite.
- **Feedback:** `python evals/check_agent.py --agent agent.py --package .
  --feedback-check` — changed campaigns, no errors for either sign.
- **Official seed 42:** PASS, net +253391.73, 20 pilots, 7 final campaigns,
  11558 contacts, cost 99998. Lower than baseline +327917.04 on this seed.
- **CSV:** two isolated plus two branch-root official generations match bytes.
  SHA-256 `152a422dd76a8ece8f17c12395dca6034fa69f827e5f2b77482cf9765b61ee1b`;
  normalized LF `290f30c0fddac57c737210051afc1ebde2c964a97900cf9d8f57ea5ce25bad15`.
- **Commit/delivery:** authorized by the user's explicit push request. The
  evaluation tool is a separate `test(evals)` commit, followed by a
  `feat(strategy)` commit, both marked `[T-10]`; T-11 audit is separate.
  Prior authorship is preserved. GitHub API confirmed the existing repository
  is private and this account has push permission. Local main remains ca44f89;
  remote main advanced to da9193b with README-only changes, not merged here.
- **Known limitations:** 5/100 holdout losses remain; seed 42 regresses. This
  tests new pilot-noise seeds on one mock business model, not hidden effects.
  Cross-channel extrapolation, history causality and pilot overlap remain
  approximations; 800 is a stopping threshold, not an exact per-candidate cap.
- **Integrator action:** review the feature branch and report before merging
  or submitting. Keep candidate strategy and regenerated CSV together. This is
  explicitly scoped cross-owner strategy/evaluation work; no changes to shared
  contracts, dependencies, agent.py or official package files.
