# T-08 — сохранённый комплект и готовность сдачи

Выбранный рабочий baseline: 4272a0787248de1481fb6b45dfcbdecee1568ae4, опубликован
в main. Эта версия остаётся доступной независимо от исхода нового эксперимента.
Код и финальный CSV в координаторской ветке2714f8f побайтно совпадают с baseline.

## Комплект

Локальный jobs/round2/delivery/beeline-baseline-4272a07.zip — полный git archive
baseline, включая код, зависимости, документацию и тесты. SHA256:
fe3966ba90113e7f95c8b1fa9bf1022c74c22faf2c24b304c262e7b61f84dd01.
Отдельный jobs/round2/delivery/submission-4272a07.csv извлечён из Git без
перегенерации и преобразования строк;10 кампаний,7 официальных колонок.
SHA256305c7d41394309b68287c035f2c89fcd08875dede274df40f08de9241e65e46a.
Manifest.json в том же локальном каталоге содержит hashes основных файлов.
ZIP и manifest не добавляются в Git; официальный пакет/данные/ключи в архив
не включены. README в baseline имеет известные замечания T-09, не выдаётся
за уже обновлённую инструкцию. После приёмки документации архив пересобирается.

## Проверки

Python hashlib/csv/zipfile + git show: code/dependencies/CSV совпали с4272a07;
10 строк кампаний,7 колонок. Полные runtime/feedback/CSV наборы не повторялись.
Для репетиции поставки архив извлечён отдельно в jobs/round2/demo-baseline,
официальный ZIP установлен штатным prepare_case.py (exit0). На этом комплекте
local_eval.py seed42: PASS,net327917,cost99998,contacts15000,4.017с,exit0.
Python3.12.14, прежние exact dependencies; ключ API удалён из окружения ребёнка.
Первый runner завершился0, но внешняя оболочка ошиблась при выводе кодировки
Windows. Один повтор с -X utf8 дал читаемый лог demo-official-seed42.log;
ошибка оболочки не скрывается и не объявляется дефектом агента. Код не менялся.
Свиде­тельства T-07:35tests,100runs,
75 положительных,25 отрицательных,mean315844.67,min-585009.70,raw errors0.
Это mock-результаты автора аудита; агрегаты координатор пересчитал отдельно.

## Платформа

23.09 около16:01 UTC+05: публичная страница HackAlem открылась в браузере,
доступ к origin подтверждён fetch/push. В браузере видна кнопка «Авторизоваться»,
сессии участника нет; приватная форма отправки и её обязательные поля не
проверены. Вкладка оставлена оператору для входа. Дедлайн17:00 получен от
оператора, а не подтверждён формой. Пароль/OTP в Git или чат не запрашивается.
Сдача не выполнена; прежний запрет сохраняется до отдельного разрешения.

## Текст для объяснения проекта

Tariff Marketing Campaign Agent, команда SapaTech, трек04/Beeline.
Агент по истории формирует гипотезы тарифов/сегментов, проверяет их пилотами
и выбирает до10 кампаний с учётом бюджета100000 и15000 контактов, включая
пилоты. Расходы и допустимость проверяются кодом. Runtime не требует API/GPU.
На100 mock-seed:75 прибыльных,mean net+315844.67; убыточность25/100 остаётся
известным ограничением. Тесты не предсказывают скрытый judge.

## Следующий шаг

Получить T-09 и при наличии времени frozen T-10. Применить пороги T-08,
обновить выбранный hash и пакет, короткое демо выбранной версии. Перед отправкой
проверить форму и точные требуемые артефакты; записать фактическую квитанцию,
время и hash только после разрешённой отправки.
