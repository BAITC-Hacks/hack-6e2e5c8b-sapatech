# Controller

Owner: PERSON_B

The Controller owns job lifecycle, Managed Agents sessions, events, runtime startup, artifact validation, and
final status. Its public request/status/result interfaces must follow `docs/contracts.md`.
